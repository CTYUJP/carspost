<!--
    <div class="post_card">
      <div class="content_post">
        <% @posts.each do |post| %>
          <%= link_to post.user.name, "/users/#{post.user.id}", class: 'post_user' %>
          <%#<%= link_to image_tag(post.image_url, class: 'post_img') %>
          <div class="post_text">
            <%= link_to post.text, "posts/#{post.id}" %>
          </div>
        <% end %>
      </div>
    </div>
-->