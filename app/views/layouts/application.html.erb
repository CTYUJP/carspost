<!DOCTYPE html>
<html>
  <head>
    <title>CARSPOST</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav_left">
            <div class="nav_title"><a href="/">
              <h1>CARSPOST</h1>
            </div>
          </div>
          <% if user_signed_in? %>
            <div class="nav_right">
              <%= link_to "投稿する", new_post_path, class: "nav_btn" %>
              <%= link_to "マイページ", "/users/#{current_user.id}", class: "nav_btn" %>
              <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete}, class: "nav_btn" %>
            </div>
          <% else %>
            <div class="nav_right">
            <%= link_to "ログイン", new_user_session_path, class: "nav_btn" %>
            <%= link_to "新規登録", new_user_registration_path, class: "nav_btn" %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © CARSPOST 2024.</p>
    </footer>
  </body>
</html>
